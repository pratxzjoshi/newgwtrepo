name: newgetmethod

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2
    - name: Get File
      run: |
        curl https://github.com/pratxzjoshi/newgwtrepo/tree/main/.github --output file.txt
